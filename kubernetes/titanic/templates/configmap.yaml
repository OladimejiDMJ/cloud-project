apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "titanic.fullname" . }}-config
data:
  DB_NAME: {{ .Values.env.db.name }}
  DB_HOST: {{ .Values.env.db.host }}
  DB_PORT: {{ .Values.env.db.port | quote }}